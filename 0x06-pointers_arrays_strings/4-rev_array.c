#include "main.h"

/**
 * main - check the code.
 * @a: an array of integers.
 * @n: the number of elements to swap.
 * Return: nothing.
 */

void print_array(int *a, int n)
{
	int i;

	for (i = 0; i < n[i]; i++)
	{
		_putchar(i);
	}
}
