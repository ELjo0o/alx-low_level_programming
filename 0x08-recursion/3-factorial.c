#include "main.h"

/**
* factorial - a function that returns the factorial of a given number.
* @n: the integar number.
* Return: (-1).
*/

int factorial(int n)
{
	int fac;

	if (n < 0)
	{
		return (-1);
	}
	else if
	{
		(n == 0)
		return (1);
	}
	fac = facrorial(n -1);
	return(n * fac);
}
