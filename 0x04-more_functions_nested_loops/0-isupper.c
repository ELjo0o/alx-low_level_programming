#include "main.h"

/**
*_isupper- search for upper or lower case for (c).
*@c: the int to test.
*Return: (1) or (0)
*/

int _isupper(int c)
{

	if (c >= 'A'; c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
