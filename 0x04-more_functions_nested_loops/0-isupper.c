#include "main.h"

/**
*_isupper- search for upper or lower case for (c).
*@c: the int to test.
*Return: (1) or (0)
*/

int _isupper(int c)
{
	int c;

	if (c >= 'A'; c <= 'Z')
	{
		_putchar("%d", c);
		return (1);
	}
	else
	{
		_putchar("%d", c)
		return (0);
	}

}
