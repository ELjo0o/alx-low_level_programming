#include "main.h"

/**
*_strlen - a function that returns the length of a string.
*@s: the charctar.
*Return: (0).
*/

int _strlen(char *s)
{

	int len;

	while (len[s] != '\0')
	len++;

	return (len);
}
